meta {
  name: Register a new user
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/signup
  body: json
  auth: inherit
}

body:json {
  {
    "email": "user@example.com",
    "password": "somepassword"
  }
}
